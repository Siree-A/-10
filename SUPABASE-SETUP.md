# เชื่อมอันดับรวมของ Research Quest

เชื่อมโครงการ lvbmzcfxseoxufemdcpi แล้วเมื่อ 25 กันยายน 2569 ตรวจอ่านอันดับ ผลรวมความคิดเห็น Anonymous Sign-In และการปฏิเสธข้อมูลไม่ถูกต้องผ่านแล้ว ผู้เล่นเลือกเข้าร่วมเพื่อเริ่มส่งแต้มออนไลน์ได้ ส่วน XP และ SWOT ส่วนตัวยังเก็บในเครื่อง คู่มือด้านล่างใช้สำหรับตั้งโครงการใหม่หรือดูแลระบบต่อไป

## เชื่อมต่อ 4 ขั้นตอน

1. เข้า [Supabase Dashboard](https://supabase.com/dashboard) ด้วยบัญชีของผู้ดูแล แล้วสร้างโครงการใหม่สำหรับเกมนี้โดยเฉพาะ เก็บรหัสผ่านฐานข้อมูลไว้กับผู้ดูแล ไม่ต้องส่งรหัสผ่านมาในแชต
2. เปิด **SQL Editor → New query** แล้ววางเนื้อหาทั้งไฟล์ `supabase/quest-setup.sql` และกด Run ไฟล์นี้สร้างตาราง กติกาคะแนน รายชื่อรหัส 80 คน และเฉลย 124 ข้อใน transaction เดียว รันซ้ำได้โดยไม่ลบคะแนนเดิม
3. ที่ **Authentication → Sign In / Providers** เปิด **Allow anonymous sign-ins** เพื่อให้เกมสร้างเซสชันโดยไม่ขออีเมลหรือรหัสผ่าน อ่าน [คู่มือ Anonymous Sign-Ins](https://supabase.com/docs/guides/auth/auth-anonymous) หากเมนูเปลี่ยนชื่อ ผู้เล่นนิรนามใช้บทบาท `authenticated` หลังเข้าสู่ระบบ ต่างจากผู้ชมที่ยังไม่มีเซสชัน
4. จาก Connect / Project Settings คัดลอก **Project URL** และ **Publishable key** มาใส่ไฟล์ `assets/quest-cloud-config.js` ตามนี้ แล้วเผยแพร่เว็บอีกรอบ:

```js
export const cloudConfig = {
  url: 'https://YOUR-PROJECT-REF.supabase.co',
  publishableKey: 'sb_publishable_...'
};
```

ใช้คีย์ Publishable หรือ legacy `anon` public key เท่านั้น คีย์นี้ตั้งใจให้ใช้ในเบราว์เซอร์ได้ ห้ามใส่ `service_role`, `sb_secret_...` หรือรหัสผ่านฐานข้อมูล ดู [ความแตกต่างของ API keys](https://supabase.com/docs/guides/getting-started/api-keys)

ก่อนเผยแพร่หลังแก้ config ให้เปลี่ยนรุ่นใน import ของ `quest-cloud-config.js` ที่ `assets/quest-cloud.js` และรุ่น import ของ `quest-cloud.js` ใน `assets/quest-club-ui.js` จากนั้นรัน `python scripts/version-assets.py` และอัปเดตรุ่น import ของ `quest-club-ui.js` ใน `assets/quest.js` ด้วย เพื่อให้ผู้ใช้ได้รับ config ใหม่ ไม่ติดแคชเก่า

หากต้องการให้ช่วยใส่ค่าหลังสร้างโครงการ ส่งมาเฉพาะ **Project URL + Publishable key** ได้ ไม่ต้องส่งคีย์ลับ

## ตรวจหลังเชื่อม

- เปิดเกมแล้วกดรีเฟรช ต้องเห็น “อันดับรวมออนไลน์” แม้ตารางยังว่าง
- ใช้รหัสตนเอง กดเข้าร่วมอันดับ แล้วตอบโจทย์หนึ่งข้อ จะแสดงส่งแต้มแล้ว เปิดอีกเครื่องกดรีเฟรชและตรวจรหัส/แต้มเดียวกัน
- ตอบข้อเดิมในวันเดียวกันไม่เพิ่มอันดับซ้ำ แม้เล่นคนละเครื่อง ถูกได้ 40 ผิดได้ 10 ผลึกชิ้นเดิมวันละ 5 ไม่รวมโบนัสคอมโบ/จบรอบของ XP
- ทดสอบหลุดอินเทอร์เน็ตแล้วเชื่อมกลับ กดรีเฟรชเพื่อส่งคิวซ้ำได้โดยไม่เพิ่มคะแนนซ้ำ คิวข้ามวันไม่เพิ่มอันดับย้อนหลัง แต่ XP ส่วนตัวคงอยู่
- ส่งแบบประเมินแบบสมัครใจ ผลรวมจะแสดงเมื่อมีอย่างน้อย 5 บัญชีนิรนามในช่วง 30 วัน จำนวนบัญชีไม่ใช่จำนวนบุคคลที่ยืนยันตัวตน
- ถ้าโหลดอันดับไม่ได้ หน้าเว็บจะระบุว่าเป็นอันดับเฉพาะเครื่อง ไม่แสดงสถานะออนไลน์สำเร็จ

หากได้ 404 ให้ตรวจว่า SQL รันครบและฟังก์ชันใน `public` เปิดให้ Data API ตามค่าโครงการ ถ้าได้ 401/403 ตรวจคีย์และ Anonymous Sign-Ins ถ้าได้ 429 ให้รอตามโควตาของ Supabase โค้ดนี้ยังไม่ได้ต่อ CAPTCHA widget หากโครงการเปิดบังคับ CAPTCHA ต้องต่อการส่ง captchaToken ก่อนเปิดใช้ในวงกว้าง

## กติกาและขอบเขตข้อมูล

- ตารางจริงอยู่ใน `quest_private` เปิด RLS และไม่ให้ `anon`/`authenticated` อ่านหรือเขียนโดยตรง เรียกได้เฉพาะ RPC ที่กำหนด ใช้ `security definer` พร้อม `search_path` ว่าง
- เซิร์ฟเวอร์ตรวจรหัส วันตามเวลาไทย คำตอบ ช่วงข้อมูล และคิดคะแนนเอง ไม่รับ XP/คะแนนจากเครื่องผู้เล่น กำหนด unique ต่อรหัส/วัน/ประเภท/ข้อ และรองรับ event UUID สำหรับลองส่งซ้ำ
- จำกัด 60 เหตุการณ์ที่เพิ่มคะแนนต่อบัญชีในหนึ่งนาที การเล่นทั่วไปไม่ถึงขีดจำกัดนี้ การป้องกันนี้ไม่ได้พิสูจน์ว่าผู้เล่นเดินถึงสถานีหรือเป็นเจ้าของรหัสจริง
- กรอกรหัสเป็นการระบุตนเองในกิจกรรมผ่อนคลาย คนอื่นอาจกรอกรหัสเดียวกันได้ ห้ามนำอันดับไปคิดคะแนนเรียน มอบรางวัลที่มีมูลค่า หรือรับรองตัวตน หากต้องการใช้แบบแข่งขันจริงต้องเพิ่มบัญชีที่ผู้จัดยืนยัน
- ชื่อบนตารางอันดับมาจาก `researchers.json` ปัจจุบัน ฐานข้อมูลเก็บเฉพาะรหัสและเหตุการณ์ ไม่คัดลอกรูปหรือชื่อเข้า Supabase
- XP ประวัติ อวตาร์ และหลักฐาน SWOT ยังอยู่ในเบราว์เซอร์ ไม่ซิงก์ข้ามเครื่อง อันดับกลางซิงก์เฉพาะเหตุการณ์หลังเลือกเข้าร่วม ไม่มีการอัปโหลด XP เก่ามาปนคะแนน
- แบบประเมินออนไลน์ไม่ส่งชื่อหรือรหัสนักเรียน แต่เก็บรหัสเซสชันนิรนามทางเทคนิค ผู้ดูแลฐานข้อมูลสามารถเชื่อมโยงเซสชันกับเหตุการณ์เกมได้ จึงไม่ควรอธิบายว่าไม่สามารถระบุตัวได้โดยสิ้นเชิง
- แบบประเมินหนึ่งรายการต่อเซสชัน/วัน ส่งซ้ำแก้รายการเดิม ผลในเครื่องเก็บเฉพาะล่าสุดของแต่ละรหัส ผู้ใช้ส่งออกผลของตนเองได้ ผู้เล่นทั่วไปอ่านผลรายบุคคลออนไลน์ไม่ได้

## ดูภาพรวมสำหรับวางแผนการสอน

ผู้ดูแลรันใน SQL Editor แล้วใช้ Export CSV ได้ ไม่มีรายชื่อในผลสรุปนี้:

```sql
select * from public.quest_board();
select public.quest_reflection_summary();

-- หัวข้อที่อยากฝึกต่อ รวม 30 วัน (ผู้ดูแลเท่านั้น)
select topic, count(*) as responses
from quest_private.reflections
where day >= (now() at time zone 'Asia/Bangkok')::date - 29
group by topic order by responses desc;
```

ค่า “เข้าใจเพิ่ม” เป็นความรู้สึกที่ผู้เล่นประเมินตนเอง ไม่ใช่ผลสัมฤทธิ์ที่พิสูจน์แล้ว ใช้ร่วมกับ SWOT และการพูดคุยเลือกหัวข้อทบทวนได้ อย่าใช้สรุปว่าเกมลดความเครียดหรือเพิ่มความรู้ได้แน่นอนจากคะแนนเหล่านี้เพียงอย่างเดียว

ผู้ดูแลควรกำหนดระยะเวลาเก็บข้อมูลให้เหมาะกับหลักสูตร สำรองก่อนลบ และทำผ่านบัญชีผู้ดูแล ไม่มีคำสั่งลบข้อมูลอัตโนมัติในชุดติดตั้ง หากปิดอันดับกลาง ให้คืน config เป็นค่าว่างและเผยแพร่เว็บใหม่ ข้อมูลเดิมในฐานข้อมูลไม่ถูกลบ

## อัปเดตข้อสอบหรือรายชื่อภายหลัง

แก้ข้อมูลต้นทางในเว็บ แล้วรัน `node scripts/build-quest-sql.cjs` เพื่อสร้าง setup ใหม่และรัน SQL ที่สร้างได้ การแก้เฉลยมีผลกับการเล่นครั้งถัดไป ไม่คำนวณคะแนนเก่าย้อนหลัง อย่าเปลี่ยน ID ของข้อเดิม เพราะใช้ผูกประวัติและตัดรายการซ้ำ

ชุดตรวจฐานข้อมูลใช้ PostgreSQL ผ่าน PGlite ในเครื่อง: `node scripts/verify-quest-db.cjs` โดยติดตั้ง `@electric-sql/pglite` และกำหนด `PGLITE_MODULE` หากติดตั้งนอกโครงการ ชุดทดสอบนี้ยังไม่แทนการตรวจการตั้งค่าและโควตาของโครงการ Supabase จริง
